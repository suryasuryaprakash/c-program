#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a;int b;
    scanf("%d%d",&a,&b);  
    printf("Addition=%d\nSubtraction=%d\nMultiplication=%d\nDivision=%d\nModulo=%d\n",a+b,a-b,a*b,a/b,a%b);
    return 0;
}

