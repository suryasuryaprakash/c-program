#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    float b,h,s1,s2,s3;
    scanf("%f %f %f %f %f",&b, &h, &s1, &s2, &s3);
    printf("Area of Triangle is %.2f\n",(b*h)/2);
    printf("Perimeter of Triangle is %.2f\n",s1+s2+s3);
    return 0;
}
